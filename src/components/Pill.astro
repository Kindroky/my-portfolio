<div class="pill"><slot /></div>

<style>
	.pill {
		display: flex;
		padding: 0.5rem 1rem;
		gap: 0.5rem;
		color: var(--gray-50);
		border: 1px solid var(--accent-regular);
		background-color: 
		rgba(52, 20, 141, 0.664);
		border-radius: 999rem;
		font-size: var(--text-md);
		line-height: 1.35;
		white-space: nowrap;
		font-weight: 400;
	}
</style>
